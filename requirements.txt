# Versions pinned from pip-freeze-runpod-worker.txt (RunPod worker 2026-01-26, Python 3.10, torch 2.9.0).
# Use these versions for reproducible installs matching production RunPod env.

# Core dependencies
# NOTE: vllm==0.12.0 is installed separately in Dockerfile BEFORE this file (it installs torch as dependency)
# torch/torchvision/torchaudio provided by vLLM (torch==2.9.0)
transformers==4.57.3
tokenizers==0.22.1
sentencepiece==0.2.1

# FastAPI and server dependencies
fastapi==0.124.2
uvicorn[standard]==0.38.0
starlette==0.50.0
pydantic==2.12.5
pydantic_core==2.41.5
aiohttp==3.10.5
httpx==0.27.2
anyio==4.6.0
h11==0.14.0
httpcore==1.0.6

# Hugging Face Hub integration
huggingface-hub==0.36.0
datasets==4.4.1
filelock==3.16.1
requests==2.32.3
packaging==25.0

# Additional utilities
numpy==1.26.4
psutil==6.0.0
ray==2.52.1

# Optional: performance and compatibility (match RunPod worker)
accelerate==1.12.0
safetensors==0.4.5
# flash-attn==2.7.4.post1 installed separately in Dockerfile with --no-build-isolation
